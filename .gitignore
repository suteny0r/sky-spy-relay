# PlatformIO
.pio/
.vscode/
.idea/

# Build artifacts (build outputs live under .pio/, already ignored above)
*.elf
*.map

# Release images in dist/ ARE tracked (flashable merged firmware)
!dist/*.bin

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# Temp
*.tmp
*.bak

# MQTT secrets (never commit)
mqtt_secrets.json
src/secrets.h

# Internal handoff doc (contains broker credentials)
HANDOFF.md
